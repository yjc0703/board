<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC
	"-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd" > 
<mapper namespace="board.dao.AdminDao"> 
	<select id="selectAdmin" resultType="board.domain.Admin">
		select id from admin
	</select>

	<select id="selectAdminMap" resultType="java.util.Map">
		select id from admin
	</select>
		
	<insert id="insertAdmin">
		insert into admin values(#{a.id})
	</insert>
</mapper>